hasas
=====

Home automation with RPis.

Local development
=================

    $ virtualenv venv
    $ . venv/bin/activate
    $ pip install -r requirements.txt

Start the master:

    $ python hasas_master.py
